version 'unspecified'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    compile project(":lib1")
    testCompile group: 'junit', name: 'junit', version: '4.11'
}
